# Docs Workflow

- Canonical documentation lives in this `docs/` folder so it renders cleanly in DocView and standard Git hosting.
- The `.trae/documents/` copies are IDE-generated artifacts and may be overwritten; treat `docs/` as the source of truth.

